extends layout.pug

block content
  h1.mt-5= title
  form(method="POST")
    .form-group
      label(for='title') Company Name
      input#name.form-control(type='text', placeholder='Enter id', name="id", value=company.id)
    button.btn.btn-primary(type='post') Find
